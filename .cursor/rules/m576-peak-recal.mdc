---
description: RECAL ????1D/2D ???]?????????????????
globs: M576Calibrator/**/Peak*.cpp,M576Calibrator/**/Peak*.h,M576Calibrator/**/Recal*.cpp,M576Calibrator/**/Recal*.h,M576Calibrator/**/M576Peak1DConstants.h,M576Calibrator/**/PmRangeValidation.*,M576Calibrator/CrossPeakTest/**
alwaysApply: false
---

# RECAL / ??????

**????????**??

- `M576Calibrator/Docs/INVARIANTS.md` ?? **INV-10..19**???? PM ??????? base??Flat ?? range??cross Y ?????
- ????/????????? `M576Calibrator/Docs/LUT_INDEXING.md`?????I?? LUT ??????? ch??

## ??????

???/????????? **????? RECAL 3/5 ????????????????**????????? **?????????????????**???? `m576-global.mdc`???????????????????????????????? CrossPeakTest ?????

## ???

- ASCII??`CRecalSession`??App??
- ?????`M576Peak1DConstants.h`
- 策略辅助：`Peak1DSweepRecenter.cpp` 中 `PlanNextRecal1DSweepAttempt`, `PlanFineRefineAfterCoarseSuccess`, `IsFlatSweepFailure`, `IsMonotoneSweepFailure`, `SuggestJumpMaxDacRange`, `IsCoarsePeakHint`, `PlanRecalYCrossResweep`, `IsRetryablePeakFailure`, `SuggestSweepRecenterNewBase`
- PM ??????`PmRangeValidation.cpp` ?? `ValidatePeakPowerInPmRange`???? RECAL 3??

## ?????????????

| ??? | ???? | INV |
|------|------|-----|
| `Flat` + `ParabolaNotDownward` | **jump-max** 至 200（`JumpFlatMax`）；@200 仍失败 → **FlatAtMaxShift**；粗扫 OK → **FineRefine** 回 UI range | INV-10 |
| `StrictInc` / `StrictDec` + `ParabolaNotDownward` | **MonoCoarseShift**：offset=200 **且** base 平移同一步；后续 **ShiftOnly**@200；粗扫 OK/hint → **FineRefine** | INV-11 |
| `ParabolaNotDownward` + 贴边 NonMono | 同 mono：**MonoCoarseShift** → **ShiftOnly** → **FineRefine** | INV-11 |
| `VertexOutOfRange` / `EdgeNotAllowed` | 启发式 + t* 混合（非 fixedRange） | INV-11 |
| ?????? -999999 / -999900 | ??? | INV-14 |
| `newBase` | int16 ???????????? | INV-13 |
| ?????? | ?? validate_code??trend??col0??offset??attempts | INV-15 |
| PM ??????? | RECAL 3 Y/X??`PmRangeMismatch` ?????? | INV-16 |
| RECAL 3/5 ?? | 6 ???? baseX/baseY | INV-18 |
| Y pre OK, cross Y fail | re-sweep mode 0 Y (`PlanRecalYCrossResweep`) | INV-19 |

- Flat **????** mono `IsRetryablePeakFailure`???? range ???????????? recenter??**INV-10/11/12**????
- 最大 attempt：`M576_PEAK1D_SWEEP_RECENTER_MAX_ATTEMPTS`（当前 12）；日志标签：`flat jump:` / `mono coarse:` / `flat shift:` / `fine refine:` / `peak retry:`
- peak??DAC ???? **??????? offset**???? range ???????? UI `m_dacRange`????
- ????????????????????????????? `no line` ??????????????? recenter??
- ???? base ???? `M576_RECAL_FW_READ_BASE_DAC` (9999)??mode0 ?? X??mode1 ?? Y@peak??INV-18????

## ????

??????/???/PM ???? ?? ???? `CrossPeakTest/main.cpp`??Release\|Win32 ?????

```cpp
// BAD ?? Flat ????? offset?????? mono recenter
if (IsRetryablePeakFailure(ParabolaNotDownward, flatProfile, n)) shiftBase();

// BAD ?? INV-13
if (iNext < 0) iNext = 0;
```
